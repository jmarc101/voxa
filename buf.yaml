version: v2
modules:
  - path: api/proto


breaking:
  use: [FILE]

lint:
  use: [DEFAULT]
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
    - SERVICE_SUFFIX
    - HTTP_RPC_ANNOTATION
